[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "student_assignment_02"
version = "0.0.0"
description = "ROS2 Python package for path planning and navigation using A* algorithm"
requires-python = ">=3.8"
dependencies = [
    "numpy",
]
